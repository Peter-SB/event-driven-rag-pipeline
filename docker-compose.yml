version: "3.9"

# ---------------------------------------------------------------------------
# Shared networking
# ---------------------------------------------------------------------------
networks:
  rag:
    name: rag-pipeline

# ---------------------------------------------------------------------------
# Volumes
# ---------------------------------------------------------------------------
volumes:
  pg_data:
  rabbitmq_data:
  redpanda_data:

# ---------------------------------------------------------------------------
# Services
# ---------------------------------------------------------------------------
services:

  # ─── Postgres + pgvector ─────────────────────────────────────────────────
  postgres:
    image: ankane/pgvector:latest
    container_name: rag-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-rag}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rag}
      POSTGRES_DB: ${POSTGRES_DB:-rag}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - rag
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-rag}"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ─── RabbitMQ (with management UI) ───────────────────────────────────────
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rag-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-rag}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS:-rag}
    ports:
      - "${RABBITMQ_PORT:-5672}:5672"
      - "${RABBITMQ_MGMT_PORT:-15672}:15672"   # Management UI
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - rag
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 10s
      retries: 10

  # ─── Redpanda (Kafka-compatible, single-node homelab) ────────────────────
  redpanda:
    image: redpandadata/redpanda:latest
    container_name: rag-redpanda
    restart: unless-stopped
    command:
      - redpanda start
      - --smp 1
      - --reserve-memory 0M
      - --overprovisioned
      - --node-id 0
      - --kafka-addr PLAINTEXT://0.0.0.0:29092,OUTSIDE://0.0.0.0:9092
      - --advertise-kafka-addr PLAINTEXT://redpanda:29092,OUTSIDE://localhost:9092
      - --pandaproxy-addr 0.0.0.0:8082
      - --advertise-pandaproxy-addr localhost:8082
    ports:
      - "9092:9092"       # External (host ↔ container) Kafka API
      - "29092:29092"     # Internal (container ↔ container) Kafka API
      - "8082:8082"       # Pandaproxy REST
      - "9644:9644"       # Admin API
    volumes:
      - redpanda_data:/var/lib/redpanda/data
    networks:
      - rag
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -q 'Healthy: true'"]
      interval: 10s
      timeout: 10s
      retries: 10

  # ─── Redpanda Console ────────────────────────────────────────────────────
  redpanda-console:
    image: redpandadata/console:latest
    container_name: rag-redpanda-console
    restart: unless-stopped
    environment:
      KAFKA_BROKERS: "redpanda:29092"
    ports:
      - "8080:8080"
    networks:
      - rag
    depends_on:
      redpanda:
        condition: service_healthy

  # ─── API (FastAPI) ────────────────────────────────────────────────────────
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rag-api
    restart: unless-stopped
    environment:
      DB_URL: "postgresql://rag:rag@postgres:5432/rag"
      RABBITMQ_URL: "amqp://rag:rag@rabbitmq:5672/"
      REDPANDA_SERVERS: "redpanda:29092"
      EVENT_BUS: "${EVENT_BUS:-postgres}"
    ports:
      - "${API_PORT:-8000}:8000"
    networks:
      - rag
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: uvicorn event_driven_rag_service.api.app:app --host 0.0.0.0 --port 8000

  # ─── CPU worker ───────────────────────────────────────────────────────────
  cpu-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rag-cpu-worker
    restart: unless-stopped
    environment:
      DB_URL: "postgresql://rag:rag@postgres:5432/rag"
      RABBITMQ_URL: "amqp://rag:rag@rabbitmq:5672/"
      REDPANDA_SERVERS: "redpanda:29092"
      EVENT_BUS: "${EVENT_BUS:-postgres}"
    networks:
      - rag
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: python -m event_driven_rag_service.worker.entrypoints.cpu

  # ─── GPU worker ───────────────────────────────────────────────────────────
  gpu-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rag-gpu-worker
    restart: unless-stopped
    environment:
      DB_URL: "postgresql://rag:rag@postgres:5432/rag"
      RABBITMQ_URL: "amqp://rag:rag@rabbitmq:5672/"
      REDPANDA_SERVERS: "redpanda:29092"
      EVENT_BUS: "${EVENT_BUS:-postgres}"
    networks:
      - rag
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    # Uncomment to map a GPU into the container (requires nvidia-container-toolkit)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    command: python -m event_driven_rag_service.worker.entrypoints.gpu
